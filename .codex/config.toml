# Basic Configuration

model = "gpt-5.5"
web_search = "cached"  # default; serves results from the web search cache - other options (live, disabled)
model_reasoning_effort = "low"
personality = "friendly" # or "pragmatic" or "none"
service_tier = "fast"

[features]
fast_mode = true

# MCP Configuration

[mcp_servers.context7]
url = "https://mcp.context7.com/mcp"
http_headers = { "CONTEXT7_API_KEY" = "ctx7sk-c96f1388-397c-4af4-8065-def9475b5fbc" }

# Advance Configuration
# Check this URL: https://developers.openai.com/codex/config-advanced